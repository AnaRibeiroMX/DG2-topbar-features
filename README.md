
  # Kebab menu + features on Mendix Data Grid 2

  This is a code bundle for features in Dg2 topbar + Kebab menu 

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
